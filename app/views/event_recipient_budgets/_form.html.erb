<%= form_with(model: @event_recipient_budget, local: true) do |form| %>
      <%= form.hidden_field :event_id %>
      <%= form.hidden_field :event_recipient_id %>

    <div class="field">
        <%= form.label :budget, "Total Budget" %><br>
        <%= form.number_field :budget, step: 0.01 %>
    </div>

    <div class="field">
        <%= form.label :spent, "Spent Budget" %><br>
        <%= form.number_field :spent, step: 0.01 %>
    </div>

    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>