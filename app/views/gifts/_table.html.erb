<table>
  <thead>
  <tr>
    <th>Name</th>
<!--    <th></th>-->
  </tr>
  </thead>
  <tbody>
  <% gifts.each do |gift| %>
    <tr>
      <td>
        <%= link_to gift.name,
                    recipient_gift_list_gift_path(@recipient, @gift_list, gift) %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

